# local state — never commit
data/attendance.db
data/attendance.db-shm
data/attendance.db-wal
data/backups/
data/gl_*.bin
data/devices.json
server.log
server.err.log
logs/
*.log
__pycache__/
*.pyc

# scratch work (diagnostics, cloned reference repos, installers)
.tmp/

# vendor installers (large, not source)
faratecno/

# raw capture files from device pulls (regenerable)
*.bin
gl_logs.json
